# 启动项目

## 环境要求

- Python 3.13+
- Node.js 18+
- pip

## 后端（端口 8000）

```bash
cd backend
pip install -r requirements.txt  # 首次需要安装依赖
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

启动成功后输出：`Uvicorn running on http://127.0.0.1:8000`

## 前端（端口 5173）

```bash
cd frontend
npm install  # 首次需要安装依赖
npm run dev
```

启动成功后输出：`Local: http://localhost:5173/`

前端已配置代理，`/api` 请求自动转发到后端 `http://localhost:8000`。

## 访问

浏览器打开 http://localhost:5173/

## 配置

后端配置项见 `backend/.env.example`，复制为 `.env` 后按需修改：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| SERVER_HOST | 127.0.0.1 | 后端监听地址 |
| SERVER_PORT | 8000 | 后端端口 |
| LLM_API_KEY | 空 | DeepSeek API Key |
| PENSIEVE_DATA_DIR | backend/data | 数据目录 |